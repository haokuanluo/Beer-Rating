# coding: utf-8
import torch
from torchtext import data
from torchtext import datasets
import random
import re
import spacy
#from __future__ import unicode_literals


def embed(words):
    NLP = spacy.load('en')
    MAX_CHARS = 20000
    list = []
    for word in words:
        list.append()

.tokenizer(comment.decode('utf8')) if x.text != " "]